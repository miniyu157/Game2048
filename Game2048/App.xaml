<Application x:Class="Game2048.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Game2048"
             xmlns:ws="https://schemas.elecho.dev/wpfsuite"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="CustomButtonStyle" TargetType="ws:Button">
            <Setter Property="Width" Value="50" />
            <Setter Property="Height" Value="50" />
            <!--<Setter Property="Margin" Value="10" />-->
            <Setter Property="Background" Value="Pink" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HoverBackground" Value="#e8a2b0" />
            <Setter Property="PressedBackground" Value="#dc98a8" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </Application.Resources>
</Application>
